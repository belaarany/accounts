image: node:latest

services:
  - mysql:latest

variables:
  MYSQL_ROOT_PASSWORD: "secret"

before_script:
  - apt-get install mysql-client

cache:
  paths:
    - node_modules/

test_mysql:
  script:
    - mysql -u root -p"$MYSQL_ROOT_PASSWORD" -h mysql
